# Dev specific overrides

nameOverride: dev-ml-api

image:
  repository: docker.io/dineshkumar20/mlops
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "dev"

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi